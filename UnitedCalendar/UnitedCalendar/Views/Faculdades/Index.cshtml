@model IEnumerable<UnitedCalendar.Models.Faculdade>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                <a class="btn btn-info" asp-action="Edit" asp-route-id="@item.IdFaculdade">Edit</a> |
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.IdFaculdade">Details</a> |
                <a class="btn btn-info" asp-action="Delete" asp-route-id="@item.IdFaculdade">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
